apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "config-injection-demo.fullname" . }}-config
data:
  text: {{ .Values.config.text | quote }}
  multiline: |
{{ .Values.config.multiline | indent 4 }}
