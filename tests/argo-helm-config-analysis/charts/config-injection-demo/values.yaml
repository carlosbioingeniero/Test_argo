replicaCount: 1

image:
  repository: quay.io/libpod/busybox
  tag: "latest"
  pullPolicy: IfNotPresent

mode: files # files | env

app:
  name: config-injection-demo

config:
  text: "hello-from-config"
  multiline: |-
    line1
    line2
